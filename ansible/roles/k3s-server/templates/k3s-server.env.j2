# /etc/default/k3s — K3s server environment file
# Managed by Ansible (k3s-server role). Do not edit manually.
# Used to document install-time flags; actual install args are in the install command.

K3S_TOKEN={{ vault_k3s_token }}
