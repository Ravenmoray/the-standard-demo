apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ingress-nginx is installed via Helm (see helm-values.yaml).
# This kustomization is a placeholder for any post-install patches.
# No kustomize resources here - ingress-nginx namespace is created by Helm.
resources: []
