apiVersion: v1
kind: Service
metadata:
  name: step-ca
  namespace: step-ca
  labels:
    app.kubernetes.io/name: step-ca
    app.kubernetes.io/version: "0.27.4"
    app.kubernetes.io/part-of: pki
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: step-ca
  ports:
    - name: https
      port: 443
      targetPort: 9000
      protocol: TCP
